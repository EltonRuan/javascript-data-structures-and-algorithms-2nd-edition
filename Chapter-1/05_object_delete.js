// Declaração de um objeto
var myObj = {name: 'Elton', age: '17'};

// Apagando a propriedade 'age' do objeto usando o operador delete
delete myObj.age;

// Exibindo o objeto após a remoção da propriedade
console.log(myObj);
